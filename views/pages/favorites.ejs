<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= (title || 'Favorite Scriptures') %> · <%= brand %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root { --ink:#0f172a; --muted:#48566a; --line:#e7ecf3; --bg:#fafbff; --accent:#0ea5e9; }
    body { margin:0; color:var(--ink); background:var(--bg); font:16px/1.6 system-ui,-apple-system,Segoe UI,Roboto,sans-serif; }
    .wrap { max-width:1100px; margin:0 auto; padding:1rem; }
    .hero { padding:3rem 0 1.5rem; background:#fff; border-bottom:1px solid var(--line); }
    .cards { display:grid; grid-template-columns:1fr; gap:1rem; padding:1.5rem 0; }
    @media (min-width:800px){ .cards { grid-template-columns:1fr 1fr; } }
    .card { background:#fff; border:1px solid var(--line); border-radius:12px; padding:1rem; }
    .ref { color:var(--muted); margin-top:.5rem; font-size:.95rem; }
  </style>
  <link rel="stylesheet" href="/assets/site.css" />
</head>
<body>
  <%- include('partials/header') %>
  <main class="wrap">
    <section class="hero">
      <h1>Favorite Scriptures</h1>
      <p class="sub">A collection of verses our community loves.</p>
    </section>
    <section class="cards" aria-label="Favorite Scriptures">
      <% (typeof favoritesScriptures!=='undefined' ? favoritesScriptures : []).forEach(function(s){ %>
        <article class="card">
          <p><%= s.text %></p>
          <p class="ref"><%= s.ref %></p>
        </article>
      <% }); %>
      <% if (!(favoritesScriptures && favoritesScriptures.length)) { %>
        <article class="card">
          <p>No favorites yet. Here are a couple to get started:</p>
          <ul style="margin:.5rem 0 0 1rem;">
            <li>“For God so loved the world…” — John 3:16</li>
            <li>“Trust in the Lord with all thine heart…” — Proverbs 3:5–6</li>
          </ul>
        </article>
      <% } %>
    </section>
  </main>
  <%- include('partials/footer') %>
</body>
</html>
