<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= (typeof title !== 'undefined' ? title : 'Favorite Scriptures') %> · <%= (typeof brand !== 'undefined' ? brand : 'Jesus Is The Christ') %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root { --ink:#0f172a; --muted:#48566a; --line:#e7ecf3; --bg:#fafbff; --accent:#0ea5e9; }
    body { margin:0; color:var(--ink); background:var(--bg); font:16px/1.6 system-ui,-apple-system,Segoe UI,Roboto,sans-serif; }
    .wrap { max-width:1100px; margin:0 auto; padding:1rem; }
    .hero { padding:3rem 0 1.5rem; background:#fff; border-bottom:1px solid var(--line); }
    .cards { display:grid; grid-template-columns:1fr; gap:1rem; padding:1.5rem 0; }
    @media (min-width:800px){ .cards { grid-template-columns:1fr 1fr; } }
    .card { background:#fff; border:1px solid var(--line); border-radius:12px; padding:1rem; }
    .ref { color:var(--muted); margin-top:.5rem; font-size:.95rem; }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <main class="wrap">
    <section class="hero hero--sm">
      <h1><%= (typeof title !== 'undefined' ? title : 'Favorite Scriptures') %></h1>
      <p class="lead">A small collection of verses that lift and guide. You can update this list anytime.</p>
    </section>

    <section class="cards" style="margin-top:1rem" aria-label="Favorite Scriptures">
      <%
        var list = (typeof favoritesScriptures !== 'undefined' && (typeof favoritesScriptures!=='undefined' && (typeof favoritesScriptures!=='undefined' && (typeof favoritesScriptures !== 'undefined' && (typeof favoritesScriptures !== 'undefined' && Array.isArray(favoritesScriptures))))) && favoritesScriptures.length)
          ? favoritesScriptures
          : [
              { ref: 'John 3:16', text: 'For God so loved the world…' },
              { ref: 'Proverbs 3:5–6', text: 'Trust in the Lord with all thine heart…' }
            ];
      %>

      <% list.forEach(function(s){ %>
        <article class="card">
          <p><%= s.text %></p>
          <p class="ref"><%= s.ref %></p>
        </article>
      <% }); %>
    </section>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
